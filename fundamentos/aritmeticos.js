const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d

// Pré Fix, post fix e In fix respectivamente
// d++
// ++d
// d + c

//Operador unário, opera apenas em um operando
// -a

const subtracao = d - b

const multiplicacao = a * b

const divisao = d / a

//geralmente usado para definir par ou impar
const modulo = a % 2


// console.log(`${soma}\n${subtracao}`)
console.log(soma, subtracao, divisao, multiplicacao, modulo)